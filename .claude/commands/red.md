Write a failing test for: $ARGUMENTS

Rules:
- Do NOT write any implementation code
- Test must fail for the right reason (missing behavior, not syntax error)
- Run the test and confirm it fails — show me the failing output
- Use the existing test framework and patterns in this repo
