Analyze all staged and unstaged changes. Group them into logical, atomic commits.
For each group:
1. Stage only the related files
2. Write a conventional commit message
3. Run tests to verify the commit is clean
4. Commit

Do NOT bundle unrelated changes. Each commit should represent exactly one logical change.