Generate Mermaid diagrams for: $ARGUMENTS

1. Analyze the feature or module
2. Generate the appropriate diagram types:
   - Sequence diagram for API flows and user journeys
   - Flowchart for business logic and decision trees
   - Class/ER diagram for data models and relationships
   - State diagram for stateful components or workflows
3. Embed them in docs/architecture/{feature-name}.md with a brief description of each
4. Validate Mermaid syntax is correct before saving

Only generate diagram types that are actually relevant — don't force diagrams that add no value.
